package com.saidur.own.amarbari.Utils;

public class Animations {
    //This fades out view.animate().alpha(0.0f);
    //This fades it back in view.animate().alpha(1.0f);
    //This moves a View down by its height: view.animate().translationY(view.getHeight());
    //This returns the View to its starting position after it has been moved somewhere else: view.animate().translationY(0);
    //You can also use setDuration() to set the duration of the animation. For example this fades out a View over a period of 2 seconds: view.animate().alpha(0.0f).setDuration(2000);
    //And you can combine as many animations as you like, for example this fades out a View and moves it down at the same time over a period of 0.3 seconds:
    //view.animate()
    //        .translationY(view.getHeight())
    //        .alpha(0.0f)
    //        .setDuration(300);

    //view.animate()
    //        .translationY(view.getHeight())
    //        .alpha(0.0f)
    //        .setDuration(300)
    //        .setListener(new AnimatorListenerAdapter() {
    //            @Override
    //            public void onAnimationEnd(Animator animation) {
    //                super.onAnimationEnd(animation);
    //                view.setVisibility(View.GONE);
    //            }
    //        });
}
